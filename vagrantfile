Vagrant.configure("2") do |config|
  config.vm.box = "ubuntu-1310-x64"
  config.vm.box_url = "http://puppet-vagrant-boxes.puppetlabs.com/ubuntu-1310-x64-virtualbox-nocm.box"
  config.vm.provision :shell, :path => "vagrant-bootstrap.sh"
  config.vm.hostname = "hola-mundo-laravel"
  config.vm.network "private_network", ip: "192.168.67.56"
  
  ##################################
  #     Comment if using windows   #

  # config.ssh.private_key_path = [ '~/.vagrant.d/insecure_private_key', '~/.ssh/id_rsa' ]
  # vagrconfig.ssh.forward_agent = true

  ##################################

  config.vm.provider "virtualbox" do |v|
    v.name = "hola-mundo-laravel"
  end

end
